name: Build

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, macos-latest]
        include:
          - os: windows-latest
            generator: "Visual Studio 18 2026"
            artifact: plugins-windows
          - os: macos-latest
            generator: "Ninja"
            artifact: plugins-macos

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Install Ninja
        if: runner.os != 'Windows'
        shell: bash
        run: |
          if ! command -v ninja &> /dev/null; then
            NINJA_VERSION="1.11.1"
            curl -sSL "https://github.com/ninja-build/ninja/releases/download/v${NINJA_VERSION}/ninja-win.zip" -o ninja.zip
            unzip -o ninja.zip -d $HOME/.local/bin/
            echo "$HOME/.local/bin" >> $GITHUB_PATH
          fi
          ninja --version

      - name: Download VST3 SDK
        shell: bash
        run: |
          cd iPlug2/Dependencies/IPlug
          if [ ! -f "VST3_SDK/base/source/baseiids.cpp" ]; then
            rm -rf VST3_SDK
            git clone https://github.com/steinbergmedia/vst3sdk.git --branch master --single-branch --depth=1 VST3_SDK
            git -C VST3_SDK submodule update --init --recursive --depth=1
          else
            echo "VST3 SDK already installed, skipping"
          fi

      - name: Clean build directory
        shell: bash
        run: rm -rf build

      - name: Configure
        shell: bash
        run: |
          cmake_args=(-B build -G "${{ matrix.generator }}" -DCMAKE_BUILD_TYPE=Release "-DIPLUG2_DIR=${{ github.workspace }}/iPlug2")
          if [ "${{ runner.os }}" = "Windows" ]; then
            cmake_args+=(-A x64)
          fi
          cmake "${cmake_args[@]}"

      - name: Build
        run: cmake --build build --config Release --parallel

      - name: Upload artifacts (Windows)
        if: runner.os == 'Windows'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: build/out/*.vst3

      - name: Upload artifacts (macOS)
        if: runner.os == 'macOS'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: build/out/*.vst3
